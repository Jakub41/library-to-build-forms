const styles = (theme) => ({
  root: {
    background: theme.palette.white,
    border: `2px solid ${theme.palette.divider}`,
    borderRadius: '5px',
  },
  inputContainer: {
    minHeight: '52px',
    background: theme.palette.white,
  },
});
export default styles;

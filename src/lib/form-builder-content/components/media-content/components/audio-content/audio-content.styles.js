const styles = (theme) => ({
  audioContainer: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'left',
    padding: theme.spacing(1, 0),
  },
  button: {
    width: 640,
  },
});

export default styles;

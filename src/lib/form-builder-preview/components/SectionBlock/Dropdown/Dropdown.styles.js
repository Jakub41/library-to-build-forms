const styles = (theme) => ({
  inputRoot: {
    backgroundColor: '#0000000A',
    '&.Mui-focused': {
      backgroundColor: '#0000001F',
    },
    padding: theme.spacing(2),
  },
  mandatoryAnswer: {
    color: 'red'
  },
});

export default styles;
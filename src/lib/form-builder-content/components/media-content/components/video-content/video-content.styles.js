const styles = (theme) => ({
  root: {
    padding: theme.spacing(1),
  },
  button: {
    width: 640,
  },
  margin: {
    margin: theme.spacing(1),
  },
  videoContainer: {
    padding: theme.spacing(2, 0),
  },
});

export default styles;

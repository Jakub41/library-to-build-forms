const styles = (theme) => ({
  gridItemContainerForHead: {
    padding: theme.spacing(1, 0),
    width: '100%',
  },
  gridItemContainerForFooter: {
    marginBottom: theme.spacing(0),
    padding: theme.spacing(1, 3, 0),
  },
  formControlLabel: {
    paddingLeft: theme.spacing(4),
  },
  divider: {
    margin: 0,
  },
});

export default styles;

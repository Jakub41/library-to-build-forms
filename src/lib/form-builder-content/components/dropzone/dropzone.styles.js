const styles = theme => ({
  root: {
    padding: theme.spacing(2),
    backgroundColor: theme.palette.background.default
  },
  button: {
    borderRadius: '4px'
  },
  icon: { width: 70, height: 70, color: 'grey' },
});

export default styles;

const styles = (theme) => ({
  imgContainer: {
    height: theme.spacing(38),
    backgroundColor: '#0000000A',
    marginTop: theme.spacing(2),
  },
  img: {
    maxWidth: '100%',
  },
});

export default styles;

const styles = (theme) => ({
  margin: {
    margin: theme.spacing(1),
  },
  button: {
    margin: theme.spacing(1),
    '& .MuiIconButton-label': {
      color: theme.palette.common.black,
    },
  },
});

export default styles;
